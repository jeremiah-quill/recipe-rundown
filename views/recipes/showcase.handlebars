
<div class="card">
    <div class="card-header text-center"><h4>{{recipe.name}}</h4></div>
    <div class="card-body">
        <div class="row">
            <div class="col-md-3 whatINeed">
                <h5 class="text-center mb-3">What I need</h5>
                    {{{arrayLoop recipe.ingredients.quantity recipe.ingredients.measurement recipe.ingredients.name}}}
            </div>
            <div class="col-md-9">
                <h5 class="text-center mb-3">What I do</h5>
                <ol>
                    {{#each recipe.instructions}}
                    <li class="ml-5">{{this}}</li>
                    {{/each}}
                </ol>
            </div>
        </div>
        <hr>
        <div class="row">
            <div class="col-md-12 text-center">
                <h5>What I think</h5>
                Notes will go here
            </div>
        </div>
    </div>
</div>